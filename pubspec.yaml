name: progressive_blur
description: "Progressive blur implementation in Flutter."
version: 0.1.0
homepage: https://github.com/kekland/progressive_blur

environment:
  sdk: ^3.5.3
  flutter: ">=3.30.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_shaders: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  shaders:
    - lib/shaders/progressive_blur.frag
